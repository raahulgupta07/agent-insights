"""Shared Memory (HYBRID_SHARED_MEMORY) — singularized cross-user agent learning.

Modules:
  scope_resolver  — agent/data-source -> the scope key(s) that gate sharing.
  sanitize        — leak firewall (P1): strip data values before a fact is shared.
  (P2 capture / P3 retrieve services land here too.)
"""
