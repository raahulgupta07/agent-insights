"""Karpathy 2nd-Brain subsystem (gated, default OFF).

Phase 4 BRAIN_READ — inject proven queries / memories into planner context.
Phase 5 QUERY_CACHE — capture proven read-only SQL (reasoning-cache).
Everything learned lands 'pending' and goes live via the dash approval gate.
"""
